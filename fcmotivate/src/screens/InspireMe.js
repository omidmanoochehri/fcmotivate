/* eslint-disable prettier/prettier */
import React from 'react';
import {Text, View} from 'react-native';

const InspireMe = ({props}) => {

  return (
    <View
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        padding: 0,
      }}>
     <Text>Inspire Me</Text>
    </View>
  );
};

export default InspireMe;
