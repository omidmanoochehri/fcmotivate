<div id="addCategoriesPage" class="container-fluid">

    <div class="row">
        <div class="col-sm-12">
            <div class="page-title-box">
                <h4 class="page-title">
                    <%= title %>
                </h4>


            </div>
        </div>
    </div>
    <!-- end row -->

    <div class="row">
        <div class="col-lg-12">
            <div class="card m-b-20">
                <div class="card-body">
                    <p class="text-muted m-b-30 "></p>

                    <form class="" action="<%= MAIN_URL %>/admin/artists" method="POST" enctype="multipart/form-data">
                        <%- action=='edit' ? `<input name="_id" type="hidden" value="${artist._id}" />` : "" %>
                            <input name="action" type="hidden" value="<%= action %>" />

                            <div class="form-group">
                                <label>First Name</label>
                                <input name="first_name" type="text" value="<%= action == 'edit' ? artist.first_name : '' %>" class="form-control" required placeholder="First Name..." />
                            </div>
                            <div class="form-group">
                                <label>Last Name</label>
                                <input name="last_name" type="text" value="<%= action == 'edit' ? artist.last_name : '' %>" class="form-control" required placeholder="Last Name..." />
                            </div>
                            <div class="form-group">
                                <label>Description</label>
                                <div>
                                    <textarea id="description" name="description"><%- action == 'edit' ? artist.description : '' %></textarea>
                                </div>
                            </div>
                            <div class="form-group">
                                <img class="musicCover" src="<%- action == 'edit' ? artist.image : '' %>" alt="<%- action == 'edit' ? artist.name : '' %>" />
                                <label>Image</label>
                                <input name="artist_image" type="file" class="filestyle" data-buttonname="btn-secondary" data-buttonText="Select">
                            </div>
                            <div class="form-group mb-0">
                                <div>
                                    <button type="submit" class="btn btn-primary waves-effect waves-light ml-1">
                                    Add
                                </button>
                                    <a href="/admin/artists" class="btn btn-secondary waves-effect">
                                    Cancel
                                </a>
                                </div>
                            </div>
                    </form>

                </div>
            </div>
        </div>
        <!-- end col -->

    </div>
    <!-- end row -->

</div>
<!-- container-fluid -->